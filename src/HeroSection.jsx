function HeroSection() {
  return (
    <section id="inicio" className="hero" aria-label="Seção inicial">
      {/* Conteúdo intencionalmente removido: apenas imagem de fundo */}
    </section>
  );
}

export default HeroSection;